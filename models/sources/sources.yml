version: 2

sources:
  - name: raw
    database: dev
    schema: main
    tables:
      - name: customers
        columns:
          - name: customer_id
            tests:
              - not_null
              - unique
      - name: orders
      - name: payments
